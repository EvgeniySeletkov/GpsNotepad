<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resource="clr-namespace:GpsNotepad.Resources"
             x:Class="GpsNotepad.Views.SignUpPage"
             Title="{x:Static resource:Resource.SignUpPageTitle}">
    <ContentPage.Content>
        
        <StackLayout Padding="40, 40, 40, 50">

            <StackLayout VerticalOptions="CenterAndExpand">

                <Entry Placeholder="Name" 
                       Text="{Binding Name}"/>
                
                <Entry Placeholder="Email" 
                       Text="{Binding Email}"/>
                
                <Entry Placeholder="Password" 
                       IsPassword="True" 
                       Text="{Binding Password}"/>
                
                <Entry Placeholder="Confirm Password" 
                       IsPassword="True" 
                       Text="{Binding ConfirmPassword}"/>

            </StackLayout>

            <StackLayout VerticalOptions="EndAndExpand">

                <Button Text="{x:Static resource:Resource.SignUpButton}"
                        Command="{Binding SignUpTapCommand}"
                        IsEnabled="{Binding IsButtonEnable}" />

            </StackLayout>
            
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>